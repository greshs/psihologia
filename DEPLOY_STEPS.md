# Пошаговая инструкция по деплою на Render.com

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com и войдите в аккаунт (или создайте новый)
2. Нажмите зеленую кнопку **"New"** или **"+"** → **"New repository"**
3. Название репозитория: `psihologia` (или любое другое)
4. Выберите **Public** (публичный)
5. НЕ ставьте галочки на "Initialize with README" (у нас уже есть файлы)
6. Нажмите **"Create repository"**

## Шаг 2: Загрузка кода на GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды (замените YOUR_USERNAME на ваш GitHub username):

```bash
cd "/home/grisha/Рабочий стол/project/psihologia"
git remote add origin https://github.com/YOUR_USERNAME/psihologia.git
git branch -M main
git push -u origin main
```

## Шаг 3: Регистрация на Render.com

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Выберите **"Sign Up with GitHub"** (самый простой способ)
4. Разрешите доступ Render к вашему GitHub аккаунту

## Шаг 4: Создание Web Service на Render

1. В Dashboard Render нажмите **"New +"** → **"Web Service"**
2. Выберите ваш репозиторий `psihologia`
3. Настройте параметры:
   - **Name**: `mac-course-backend` (или любое другое имя)
   - **Region**: выберите ближайший (например, Frankfurt)
   - **Branch**: `main`
   - **Root Directory**: `backend` ⚠️ ВАЖНО!
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: `Free`
4. Нажмите **"Create Web Service"**

## Шаг 5: Ожидание деплоя

Render начнет деплой. Это займет 2-5 минут. Вы увидите логи процесса.

## Шаг 6: Получение URL

После успешного деплоя Render даст вам URL вида:
```
https://mac-course-backend.onrender.com
```

⚠️ **Сохраните этот URL!** Он понадобится для React приложения.

## Шаг 7: Проверка работы

Откройте в браузере:
- API: `https://ваш-бэкенд.onrender.com/api/content`
- Админка: `https://ваш-бэкенд.onrender.com/admin`

Должна открыться админка для редактирования контента!

## Шаг 8: Обновление React приложения

1. Откройте файл `my/src/config/wordpress.js`
2. Замените URL API на ваш Render URL:

```javascript
export const WORDPRESS_CONFIG = {
  API_URL: process.env.REACT_APP_API_URL || 'https://ваш-бэкенд.onrender.com/api',
  USE_FALLBACK: process.env.REACT_APP_USE_FALLBACK !== 'false',
};
```

Или создайте файл `my/.env.production`:
```
REACT_APP_API_URL=https://ваш-бэкенд.onrender.com/api
```

3. Пересоберите React приложение:
```bash
cd my
npm run build
```

4. Задеплойте React приложение на GitHub Pages (как мы делали раньше):
```bash
npm run deploy
```

## Готово! 🎉

Теперь у вас:
- Бэкенд работает на Render: `https://ваш-бэкенд.onrender.com`
- Админка доступна: `https://ваш-бэкенд.onrender.com/admin`
- React приложение использует этот API

---

## Примечания

- На бесплатном плане Render сервисы "засыпают" после 15 минут неактивности
- Первый запрос после "сна" может занять до 30 секунд (cold start)
- Если нужно, можно настроить "wake up" через ping сервисы

